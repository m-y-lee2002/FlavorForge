# Use MySQL database for testing
spring.datasource.url=jdbc:mysql://localhost:3306/MyCookBook
spring.datasource.username=root
spring.datasource.password=Keyboard123!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate settings for testing
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# Set the Hibernate dialect for MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Logging level for testing (optional)
logging.level.root=DEBUG